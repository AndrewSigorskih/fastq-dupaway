#pragma once
#include <stdlib.h>

namespace constants
{
    const ssize_t ONE_MB = 1024L * 1024L;
    const ssize_t HUNDRED_MB = 100L * ONE_MB;
    const ssize_t TWO_GB = 2L * 1024L * ONE_MB;
    const int DIRNAME_LEN = 10;
}

namespace params
{
    inline int PREFIX_LEN = 32;
}
